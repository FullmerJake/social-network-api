const { Thought } = require('../models');

const thoughtController = {
    // GET to get all thoughts
    // GET to get a single thought by its _id
    // POST to create a new thought, not forgetting to push the created thought's _id to the associated user's thoughts array field
    // PUT to updated a thought by its _id
    // DELETE to remove a thought by its _id

    // POST to create a reaction stored in a single thought's reactions array field
    // DELETE to pull and remove a reaction by the reacion's reactionId value
    
};

module.exports = thoughtController;